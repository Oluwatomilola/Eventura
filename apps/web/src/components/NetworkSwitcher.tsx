'use client'

/**
 * NetworkSwitcher Component
 *
 * Displays REOWN AppKit's network selection button.
 * Allows users to switch between Base and Base Sepolia networks.
 */

export function NetworkSwitcher() {
  return <appkit-network-button />
}
